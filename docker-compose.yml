services:
  sift:
    container_name: findevil-sift
    build:
      context: .
      dockerfile: Dockerfile.sift
    volumes:
      - ./cases/e2e_test:/evidence:ro
      - ./workspace:/workspace
    network_mode: none
    restart: unless-stopped
